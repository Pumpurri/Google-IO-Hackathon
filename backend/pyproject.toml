[project]
name = "worldmog-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "pydantic-settings",
  "python-dotenv",
  "openai",
  "google-genai",
  "httpx",
  "pillow",
  "websockets",
  "rocketride",
]
